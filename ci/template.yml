.tags:
  tags:
    - stryker
.no_run:
  when: never

.image:
  image:
    name: mcr.microsoft.com/dotnet/sdk:8.0
.base:
  script: 
    - echo "This is template inherited"
    - echo "$CI_RUNNER_DESCRIPTION:$CI_JOB_STAGE:$CI_JOB_NAME"
    
